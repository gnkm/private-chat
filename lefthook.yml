# yaml-language-server: $schema=https://json.schemastore.org/lefthook.json

pre-commit:
  commands:
    osv-scanner:
      run: osv-scanner scan -r --format=vertical --verbosity=error .
    biome:
      run: pnpm exec biome check .
    test:
      run: pnpm test
